<Window x:Class="CafManagerConection.App.Views.ConnectionHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Historial de conexiones"
        Height="560" Width="820"
        MinHeight="360" MinWidth="620"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Lateral}"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Historial de conexiones"
                   Style="{StaticResource Titulo}"
                   Margin="0,0,0,4" />

        <TextBlock Grid.Row="1"
                   Style="{StaticResource Tenue}"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap"
                   Text="Se anota un evento por intento, con cuánto duró la sesión. Se conservan los últimos 100 eventos de cada conexión." />

        <Border Grid.Row="2" Style="{StaticResource Tarjeta}" Padding="0">
            <Grid>
                <TextBlock x:Name="_vacio"
                           Text="Todavía no hay conexiones registradas"
                           Style="{StaticResource Tenue}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Collapsed" />

                <DataGrid x:Name="_grilla"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserSortColumns="True"
                          CanUserResizeRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          SelectionMode="Single"
                          Background="Transparent"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Conexión"
                                            Binding="{Binding Conexion}"
                                            Width="2*" />
                        <DataGridTextColumn Header="Cuándo"
                                            Binding="{Binding Cuando}"
                                            Width="Auto" />
                        <DataGridTemplateColumn Header="Resultado" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="7" Height="7"
                                                 Margin="0,0,7,0"
                                                 VerticalAlignment="Center"
                                                 Fill="{Binding Color}" />
                                        <TextBlock Text="{Binding Resultado}"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Duración"
                                            Binding="{Binding Duracion}"
                                            Width="Auto" />
                        <DataGridTextColumn Header="Detalle"
                                            Binding="{Binding Detalle}"
                                            Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <TextBlock x:Name="_resumen"
                       Style="{StaticResource Tenue}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Actualizar"
                        Style="{StaticResource BotonSecundario}"
                        Width="100"
                        Margin="0,0,8,0"
                        Click="AlActualizar" />

                <Button Content="Cerrar"
                        Style="{StaticResource BotonPrimario}"
                        Width="100"
                        IsCancel="True"
                        Click="AlCerrar" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
