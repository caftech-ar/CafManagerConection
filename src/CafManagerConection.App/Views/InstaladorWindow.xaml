<Window x:Class="CafManagerConection.App.Views.InstaladorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        SizeToContent="Height" Width="460"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Title="Elegir el instalador"
        Background="{DynamicResource Fondo}">

    <StackPanel Margin="18">
        <TextBlock x:Name="_encabezado"
                   TextWrapping="Wrap"
                   LineHeight="19"
                   Margin="0,0,0,14" />

        <StackPanel x:Name="_opciones" />

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar"
                    Style="{StaticResource BotonSecundario}"
                    IsCancel="True" MinWidth="100"
                    Click="AlCancelar" />

            <Button Content="Descargar"
                    Style="{StaticResource BotonPrimario}"
                    IsDefault="True" MinWidth="100" Margin="8,0,0,0"
                    Click="AlAceptar" />
        </StackPanel>
    </StackPanel>
</Window>
